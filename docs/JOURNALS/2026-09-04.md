# 2026-09-04 — Session 30 — Fase 1 sub-step 4: rewrite implementation contracts to unified composite model

## Scope

Rewrite the deferred implementation contracts (LEG-040 SequenceAgent,
LEG-041 ParallelAgent, LEG-042 fan-in, LEG-043 examples, LEG-051 parent
continuation, LEG-053 final-result delivery) to the unified `type: composite`
model. Eliminate every reference to the old `SequenceAgent`/`ParallelAgent`
split, `kind: sequence|parallel`, and "two composite agent classes".

## Work done (per concern)

- **`docs/CONTRACTS/LEG-040-composite-agent.md`** (NEW, replaces
  `LEG-040-sequence-agent.md`): rewritten from scratch — one unified composite
  runner, `type: composite` + `branches`, fan-out deposits each branch's own
  loader-resolved `level_route`, fan-in joins per `(task_id, branch_id)`,
  builds `output_as`, re-keying at every hop. Acceptance criteria aligned to
  LEG-044 (PLAN.md). No `SequenceAgent`, no `ParallelAgent`, no `kind:
  sequence|parallel`.
- **`docs/CONTRACTS/LEG-040-sequence-agent.md`**: DELETED (replaced by
  `LEG-040-composite-agent.md`).
- **`docs/CONTRACTS/LEG-041-parallel-agent.md`**: DELETED — content absorbed
  into LEG-040-composite-agent.
- **`docs/CONTRACTS/LEG-042-fanin-gathering-queue.md`**: rewritten —
  `state:parallel:<class>` → `state:<composite>`; fan-in dedupe per
  `(composite, task, branch_id)`; removed "sequence is forward-only" and all
  ParallelAgent references.
- **`docs/CONTRACTS/LEG-043-examples-composites.md`**: rewritten — examples
  are single-branch and multi-branch composites on the same unified runner;
  removed "sequence (steps chained) and a parallel (fan-out/join)" as
  separate classes.
- **`docs/CONTRACTS/LEG-051-parent-continuation.md`**: rewritten — "sequence
  *and* parallel" → "every composite frontier"; removed two-class framing.
- **`docs/CONTRACTS/LEG-053-ultimate-return-delivery.md`**: rewritten —
  "sequence step in the last position" → "a step in the last position";
  flow-end generalized; no class-specific framing.
- **`docs/PLAN.md`**: LEG-040 and LEG-041 marked as ~~superseded~~ by
  LEG-044; LEG-042 and LEG-043 descriptions updated to the unified model.

## Logging added

- None. Contracts and docs only; no code change.

## Tests run

- Docs-only change; no code or tests touched. Suite unaffected (131 passed
  from Session 23).

## Decisions taken

- The unified composite model (`type: composite` + `branches`, one runner) is
  the only model. LEG-040 and LEG-041 are superseded; their contracts are
  replaced by a single LEG-040-composite-agent contract.
- Historical journals (2026-08-28 through 2026-09-03) are not edited — they
  record the shipped SequenceAgent/ParallelAgent code that the unified design
  supersedes.
- Remaining mentions of `SequenceAgent`/`ParallelAgent` in the active tree
  (PLAN.md supersession notes, LEG-040-composite/LEG-043 negation clauses)
  are intentional denials of the old model, not residual drift.

## Known issues / open points

- All six implementation contracts are now consistent with the unified model.
- Fase 1 (documentation rewrite) is complete for the contracts. The remaining
  Fase 3 work is the **code**: schema1/loader/composite runner/route builder
  (per Session 22 plan, one authorized step at a time).

## Next steps

- Fase 3 step 1: Schema 1 (`schema1.py`): `AgentKind` →
  `tool|linguistic|composite`; `branches` field; drop `sequence`/`parallel`.
  Requires maintainer authorization.
