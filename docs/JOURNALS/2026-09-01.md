---
# 2026-09-01 — Session 15: finish the Schema 2 doc reconciliation (Fase 1 completion)

## Scope
The Fase 1 (docs) reconciliation landed in session 14 had only covered
LEG-010/011/040/041/050/051/053. This session completed the Schema 2
reconciliation of the remaining contracts that still described the v1 model
(`results:{task_id}` board / `client:{task_id}` / `ultimate_return_agent_id`):
LEG-012, LEG-014, LEG-016, LEG-024, LEG-026, LEG-032, plus the stale
acceptance wording in `docs/PLAN.md`. Docs-only; no code changed.

## Work done (per issue)
- Verified the actual gap against the Fase 1 commit (`fc6c413`): it touched
  only LEG-010/011/040/041/050/051/053; the six remaining v1-voiced contracts
  were untouched. The tree was clean and the Fase 2 commit (`eadf9c7`) already
  implemented Schema 2 in code. Maintainer confirmed: "Actualiza todo, todo al
  schema 2".
- **LEG-014 (mini-manager contract)** — re-voiced to Schema 2: `submit` seeds
  the level-1 root token with `end_of_level_queue = result_queue_key(task_id)`
  (the final-result queue); the class closing the flow at `level == 1` deposits
  the final result there; no `results:{task_id}` board, no `client:{task_id}`
  queue, no `ultimate_return_agent_id`. `status` reads via a non-destructive
  `peek`.
- **LEG-024 (mini-manager impl)** — same re-voicing (final-result queue,
  submit-seeded `end_of_level_queue`, `status` `peek`).
- **LEG-026 (E2E example)** — result lands in the task's final-result queue
  (`result:<task_id>`) at flow close, read back via `status`.
- **LEG-032 (linguistic-tool example)** — final result lands in the task's
  final-result queue; "board scoping" → "final-result-queue scoping".
- **LEG-016 (naming & errors)** — agent ids are class-based (Schema 2), no
  `client:` reserved family; task id is `{uuid}` and its final result is
  addressed by `result_queue_key(task_id)` → `result:<task_id>`.
- **LEG-012 (primitives, SUPERSEDED)** — removed `results:{task_id}` from the
  Board primitive list (Schema 2 delivers the root result to the final-result
  queue, not a board).
- **`docs/PLAN.md`** — re-voiced the still-v1 acceptance lines: LEG-026
  (final-result queue), LEG-050 (title + accept: submit-seeded
  `end_of_level_queue` = final-result queue), LEG-053 (title + accept:
  flow-end at `level == 1` delivers the final result, distinct from branch
  closes).

## Logging added
- None (docs-only; rule 11 applies to shipped code, nothing shipped this turn).

## Tests run
- Fixture suite **108 pass**; `ruff` clean; `pyright` **0 errors** (docs-only,
  no code touched — re-run to confirm the green baseline holds).
- Grep cross-check: no active v1 references remain in `docs/CONTRACTS/*.md`
  or `docs/PLAN.md`; the only remaining mentions of `results:`/`client:`/
  `ultimate_return_agent_id` are "removed/superseded/no hay" explanatory notes,
  matching the style already used in LEG-011/050/051/053 and
  `ARCHITECTURE.md`/`AGENT_LIFECYCLE.md`.

## Decisions taken
- All six remaining v1-voiced contracts are now aligned to the Schema 2 model
  (`final-result queue result:<task_id>`, submit-seeded `end_of_level_queue`,
  no boards / `client:` family), completing the Fase 1 doc reconciliation.
- Same style as Fase 1: historical v1 names are retained only inside explicit
  "removed/superseded" notes, never as active specification.

## Known issues / open points
- The v1 CLOSED contracts not part of the Schema 2 path (e.g. LEG-021/022/030/
  031/072 wording) were intentionally left as-is per prior session policy; they
  are re-voiced on contact when their own issues are worked.
- `naming._RESERVED_AGENT_PREFIX = "client:"` and `is_reserved_agent` remain as
  harmless vestigial helpers (documented in the Fase 2 journal); safe to drop
  when S1/S3 land.
- `manager.results_board` is a raising stub (loud failure if any old caller
  remains) — retained intentionally.

## Next steps
- Await maintainer review of this doc reconciliation. On approval, commit this
  turn (rule 5: maintainer closes; nothing committed unjournaled — journaled
  here).
- After approval, proceed to the pending Schema 1 and Schema 3 code migrations
  (patterns → S1, tools registry → S3), then R-4 composites — per the HANDOFF
  priority order, contract-first.

---

# 2026-09-01 — Session 15b: align ALL docs/contracts/plans to Schemas 1/2/3 (no code touched)

## Scope
The maintainer clarified the directive (turns): "Es arreglar lo que tenemos
ahora, no lo que falta por implementar, y los planes que existen revisarlos
para alinearlos... para cuando se comience a implementar todo no exista
problemas." Confirmed the 3 schemas as reference (§4.11: S1 pattern · S2
token/message · S3 tools). This session aligned the docs/contracts/PLAN/README
that still described v1 constructs, WITHOUT implementing the S1/S3 code
migrations (that is the subsequent planned work). Docs-only; no code changed.

## What the alignment fixed
- **`docs/PLAN.md`** — re-aligned the issue bullets that still carried rejected
  v1 vocabulary:
  - **LEG-010 bullet**: replaced the stale `{from:}`/`bind:`/`emit:` /
  "parallel binds only from input_as" description with the S1 terse call
  (`parameters: {arg: dotted.path | literal}`, no from/value/default/bind/emit),
  mandatory symmetric contracts on every agent, composite output = agent's
  implementation, contract compatibility (not exact subset). Accept lines
  rewritten to the S1 equivalents.
  - **LEG-013 bullet**: v1 `tool_type`/input-output-schema validation → Schema 3
  `available_tools:{implementation, policy}`; no output declaration; static =
  flow-against-itself, execution-time = against the tool.
  - **LEG-021 / LEG-022 bullets**: re-voiced to S1 (loader + ToolAgent) with
  Schema 2/Schema 3 references; removed "board write"/`output_as` board wording.
- **`docs/CONTRACTS/LEG-013`** — re-vozar a Schema 3 (the `available_tools`
  declaration), with an explicit note that the current `legio.tools` registry is
  still v1 and this is the target contract pending migration.
- **`docs/CONTRACTS/LEG-021`** (patterns loader) and **LEG-022** (ToolAgent) —
  re-voiced to Schema 1 (one agent spec, mandatory contracts, terse
  `parameters`, chain-wide resolution, no auto-naming/self-execution/
  queue-name materialization in the loader) + Schema 2/3, with honest
  "implementation state" notes (current v1 code).
- **`docs/CONTRACTS/LEG-072`** (pattern compile) and **LEG-030** (linguistic) —
  re-framed to the Schema 1 mandatory `output_schema` mini-grammar and
  `kind: linguistic` `prompt` + prompt-var↔`input_schema` coherence; removed
  v1 `linguistic: {model, prompt}` / self-execution / `out` frame wording.
- **`README.md`** — State section updated to the current reality: R-3 shipped on
  the Schema 2 engine (no `results` board / `client:` pseudo-agent), contracts &
  PLAN aligned to Schemas 1/2/3, and **S1/S3 code migration explicitly flagged
  as pending** (the next planned work stream). No false claim that S1/S3 are
  implemented.

## What was NOT done (by design)
- **No code migration.** `src/legio/patterns/*`, `src/legio/tools.py`,
  `agents/tool_agent.py`, `agents/linguistic_agent.py` remain the v1
  implementations. Per the maintainer, this is the "lo que falta por
  implementar" — deliberately deferred; its contracts now state the S1/S3
  targets so the migration is unambiguous.
- The Schema 3 runtime decisions (how `implementation` is loaded — import vs
  programmatic registration — and the async mechanism) remain explicitly
  undecided (addendum O / Session 13); the LEG-013 contract records them as
  decided at implementation, not by the schema.

## Logging added
- None (docs-only; nothing shipped).

## Tests run
- Fixture suite **108 pass**; `ruff` clean; `pyright` not re-run (no code
  touched; previous baseline 0). Confirmed nothing regressed by the doc changes.

## Known issues / open points
- The S1/S3 code migration (LEG-010/021/022/072 loader+ToolAgent+compile, and
  LEG-013 registry) is the next planned work stream, per issue, contract-first
  — the contracts written this session are its spec.
- The runtime loading + async mechanism for Schema 3 tools is still undecided
  (recorded in LEG-013 as decided at implementation).

## Next steps
- Maintainer review of this alignment. On approval, commit this turn (rule 5).
- Then proceed to the S1/S3 code migration per the re-aligned contracts, and
  review/adjust all remaining issues before implementing, per the maintainer's
  directive.

---

# 2026-09-01 — Session 15c: audit-driven fixes (results of the independent re-check)

## Scope
The maintainer asked for an independent re-check of the alignment done in
sessions 15/15b. A sub-agent audited all docs/contracts/PLAN/README/ARCHITECTURE
against the three schemas (S1/S2/S3, normative in `AGENT_LIFECYCLE.md` §4.11)
and confirmed the alignment was correct. It found 4 residual items, 2 of which
were ACTIVE contradictions (not mere notes) — all fixed in this turn.

## What the audit confirmed (no change needed)
- README State: correct — R-3 on the Schema 2 engine, contracts/PLAN aligned to
  the 3 schemas, S1/S3 code migration flagged PENDING; nothing falsely claims
  S1/S3 is implemented.
- All modified contracts + the checked unmodified contracts: every v1 term is
  inside a permissible "removed/superseded/no hay" note; no ACTIVE v1 schema
  terms remain.
- The "implementation state (pending)" notes in LEG-013/021/022 are honest and
  match the actual v1 code.
- Code files (`patterns/*`, `tools.py`, `tool_agent.py`, `linguistic_agent.py`)
  remain v1 — expected per the maintainer's directive (docs first, code
  deferred).

## Fixes applied (this turn, docs-only)
1. **`docs/ARCHITECTURE.md` §5 The Tool** — removed ACTIVE v1 statements that
   contradicted Schema 3:
   - Definition: "only `input_schema`/`output_schema` (pydantic)" → tool does
     **not** declare input/output capacity; the consuming agents declare it via
     `output_as`/`output_schema`.
   - Runtime (ToolAgent): removed "the tool declares its input/output capacity
     via `input_schema`/`output_schema`"; now resolves `tool: <name>` +
   `available_tools` (loaded dynamically), resolves terse `parameters` against
   the carried state, validates against the tool's signature **in execution**
   (not verifiable at load), bounds output under the agent's
   `output_as`/`output_schema`.
2. **`docs/PLAN.md` LEG-014 bullet** — lead-in "Mini-manager contract (v1):
   `submit`/`status` over boards" → "(Schema 2): `submit`/`status` delivering the
   root result to the task's final-result queue".
3. **`docs/CONTRACTS/LEG-031`** — `resolve_template(template, board_entry)`
   (stale v1 "board" parameter name) → `carried_state`; body clarified that the
   carried record travels in the message's single `payload` container (S2).

## Tests run
- Docs-only changes; fixture suite remains 108 pass, ruff clean (verified earlier
  this session; no code touched since).

## Known issues / open points
- The S1/S3 code migration (patterns → S1, tools registry → S3) remains the next
  planned work stream, contract-first, per the re-aligned contracts.

## Next steps
- Maintainer review of the full alignment (sessions 15, 15b, 15c). On approval,
  commit (rule 5).
- Then proceed to the S1/S3 code migration and, per the maintainer's directive,
  review/adjust all remaining issues before implementing.
