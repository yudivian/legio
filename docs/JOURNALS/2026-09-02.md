# 2026-09-02 — Session 16

## Scope

R-2/R-3/R-4 alignment: honour the maintainer's non-negotiable rule that
**nothing** of the invented lease/retry/re-queue/orchestrator model may exist in
specs *or* code — because leaving any trace of it makes the programmer "invent
errors and things that don't exist". Goal: remove every trace of the R-6
lease/reaper/retry/DLQ coupling across the whole repository and re-anchor
`AgentBase` to the approved LEG-023 polling-only runner and Schema 2.

The maintainer had repeatedly flagged that my work drifted from the approved
definitions (2026-08-31 addenda/`docs/ARCHITECTURE.md` Schema 2), that I
prematurely authored R-6 specs (DRAFT, unapproved, contradicting Schema 2), and
that I failed to journal several turns. This session removes that drift for
real, repo-wide.

## Work done (per concern)

- **Code — `src/legio/agents/base.py`**: simplified `AgentBase` to the
  LEG-023 polling-only runner. Removed the per-dispatch lease/lock
  (`db.lock` + `lease.acquire/release`), `_heartbeat`, `_take_due`/`_is_due`/
  `_item_id` (the `next_run_at` gate + re-queue rotation), `_attempts`,
  `_should_retry`/`_retry` (re-queue to own queue), and the `RetryGuard` hook.
  Kept the approved core: `run(max_steps)` loop → `process_next` pops
  destructively (`queue.get(block=False)`) → `model_validate` → `_emit(start)`
  → `_handle` → `_route_outcome` (positional Schema 2 advance or level-closer)
  → `_emit(step_done/step_error)`; the `monitor` hook (observability) is kept,
  the `retry_guard` hook is gone. A raised step is surfaced as an `error`
  result, never re-queued (rule 9).
- **Code — agent subclasses**: removed the `lease_ttl` pass-through from
  `tool_agent.py`, `linguistic_agent.py`, `sequence_agent.py`, `parallel_agent.py`.
- **Code — `src/legio/errors.py`**: removed the retry-feeding surface —
  `retriable()` and the `retriable` export; dropped `_recoverable`/`_retriable`
  flags; reworded the module and class docstrings so they no longer claim to
  "drive retry/DLQ policy (R-6)". Kept the LEG-016 typed taxonomy
  (`LegioError`/`RecoverableError`/`UnrecoverableError`, `code`, `recoverable`)
  which `loader.py`/`sequences.py` legitimately use for validation failures.
- **Tests**: rewrote `tests/test_leg023_agentbase.py` (removed `retry_guard`
  hook, `lease_ttl`, and the retry test; the failure path is now the
  never-silent error-result test) and `tests/test_leg016_naming_errors.py`
  (removed `retriable` import + its test).
- **Specs — deleted the 5 R-6 DRAFT contracts** (never approved, contradicting
  Schema 2): `LEG-060-leases-reaper.md`, `LEG-061-retry-fields.md`,
  `LEG-062-dlq.md`, `LEG-063-parallel-fail-policy.md`,
  `LEG-064-resilience-scenarios.md`.
- **Docs — removed the R-6 rasante**: dropped "R-6 Resilience" from the
  rasantes chain in `docs/PLAN.md` (and README roadmap) and removed the R-6
  issues section; cleaned the LEG-012/020/023/041/080/082 plan entries of the
  lease/`next_run_at`/reaper acceptance wording.
- **Docs — cleaned every dependent spec/design referencing the removed model**:
  `LEG-023-agentbase-run` (now pytest-verified polling-only), `LEG-012`,
  `LEG-014`, `LEG-015`, `LEG-016`, `LEG-020`, `LEG-022`, `LEG-030`, `LEG-040`,
  `LEG-041`, `LEG-043`, `LEG-080`, `LEG-082`, `ARCHITECTURE.md` (§2 Queue/Lock,
  §4 base, §5 tool, §8 rewritten to never-silent/no-lease failure model,
  federation outbox), `AGENT_LIFECYCLE.md` (removed the TaskManager execution
  lease `tm_lease`, the stale-lease reaper/`executor_died` recovery, DLQ/retry;
  kept `next_run_at` as the TaskManager **submit scheduling** field per rule 8),
  `CONTRIBUTING.md` (rules 5/7 and CI), `AGENTS.md` (rule 9 dropped the
  "exhausted attempts go to a DLQ" clause), README.

## Logging added

- `base.py`: the surviving events are unchanged (`agent idle`, `agent run`,
  `agent step error`, `agent done`, `agent advance`, `agent finish`,
  `agent branch close`). Removed the `agent lease contention`, `agent heartbeat`
  and `agent retry` events that accompanied the deleted lease/retry/re-queue
  mechanism. No new events were needed for the removal; existing observability
  points were kept intact (rule 11).

## Tests run

- `uv run python -m pytest -q` → **126 passed**.
- `uv run ruff check src tests` → **All checks passed**.
- `uv run pyright` → **0 errors, 0 warnings, 0 informations**.

## Decisions taken

- **No lease, no retry, no re-queue in the dispatch**, anywhere (code + specs +
  design). An item is popped once and routed; a raised step is surfaced as an
  `error` result, never re-run. At-least-once execution and its reaper/DLQ
  machinery are removed (invented, unapproved).
- **TaskManager `next_run_at` as a submit-scheduling field is kept** (rule 8,
  approved) — it gates *when* a task runs, it is not a dispatch lease.
- **`recoverable`/`RecoverableError`/`UnrecoverableError` remain** as the
  approved LEG-016 typed error taxonomy (used by pattern loader/sequences);
  only the retry-feeding `retriable` surface and flags were removed.
- Rasante numbering R-7..R-10 are kept as-is (they mirror the GitHub issue
  labels R-0..R-10); only the R-6 label/issues were removed.

## Known issues / open points

- This turn is authored by the agent and reflects the maintainer's ruling;
  maintainer approval/review is expected before closing anything.
- The R-4 work (LEG-040/041, already green and previously committed) is
  unaffected by the runner simplification — re-verified by the full suite.
- `docs/AGENT_LIFECYCLE.md` and `docs/ARCHITECTURE.md` now document the
  *absence* of lease/reaper/retry (e.g. "Dispatch has no lease or reaper") —
  intentional, so the decision is recorded, not re-introduced.

## Next steps

- Await maintainer approval of this alignment; confirm the R-6 rasante removal
  and the `next_run_at`-as-scheduling-field distinction.
- After approval, continue the earlier design review (fan-in `branch_id`
  slot-identity) on top of the now-aligned, lease-free runner.
- Journal every turn without exception (rule 3).

---

# 2026-09-02 — Session 16b — obsolete-history purge (decisions live in text, not addenda)

## Scope

The maintainer's standing, emphatic rule (restated this session): eliminate the
"obsolete-history" practice project-wide — specs/contracts/design must contain
**only current truth**. No "SUPERSEDED", "REVISED <date>", "(v1)", "(removed)",
"old …", "target contract", "Session N", addenda-letter provenance, or any
other trace of previous decisions; new decisions must be reflected **in the
text**, not recorded as addenda. History belongs only in `docs/JOURNALS/`.
The maintainer confirmed the principle again explicitly: *"No deben existir
addendum de ningun tipo, las decisiones nuevas deben reflejarse en los textos,
no con addendums"* and directed a full strip of the AGENT_LIFECYCLE provenance
(both my scoping questions answered "Haz 1").

## Work done (per area)

- **Code — LEG-048 label removed from source docstrings** (references to the
  deleted `LEG-048` "no invented substrate" issue label, which had no live
  contract/issue): `src/legio/agents/base.py`, `src/legio/manager/__init__.py`
  (×3), `src/legio/naming.py` (×3). Reworded to plain "native beaver / no
  invented substrate layer" without the dangling issue number.
- **Tests — LEG-048 removed** from docstrings of `test_leg014_manager.py`,
  `test_leg030_linguistic_agent.py`, `test_flow_integration_decoupled.py`,
  `conftest.py`, `test_leg023_agentbase.py`, `test_leg025_worker.py`,
  `test_leg022_toolagent.py`, `test_leg032_example_summarize.py`
  (×1 each); removed "(v1)" from `test_leg016_naming_errors.py` and
  `test_leg017_security.py` docs.
- **Contracts — status/history markers removed** so each is current-only:
  - `LEG-012` & `LEG-020` (rewritten earlier, verified); `LEG-010`, `LEG-011`,
    `LEG-013`, `LEG-014`, `LEG-015`, `LEG-016`, `LEG-017`, `LEG-021`,
    `LEG-022`, `LEG-023`, `LEG-053` (status → APPROVED / schema labels kept,
    revision notes removed, verified green).
  - Titles de-historicized: `LEG-050` `(reconciled to Schema 2)` → `(Schema 2)`;
    `LEG-051` same.
  - **Addenda stripped (maintainer: none of any kind),** keeping the surrounding
    sentence as current truth: `LEG-010` (AY, AT ×2), `LEG-011` (AV, AM,
    AJ/AL), `LEG-013` (O / Session 13), `LEG-014` (AV), `LEG-041` (AV),
    `LEG-050` (AJ/AL, AM, AV), `LEG-051` (AV, AJ/AL), `LEG-053` (AV, AJ/AL).
- **`docs/ARCHITECTURE.md`** — removed the "Reconciled against … Session 14 …
  old … superseded" blockquote (§3 FlowToken); removed `addendum AL`,
  `addenda AJ/AL` and the `addendum AV` provenance (keeping the generalized end
  rule concept).
- **`docs/PLAN.md`** — rewrote the LEG-012/020 entries that said "Superseded by
  the native-beaver substrate (LEG-048)" to current truth; removed
  `(v1)`/`(revised → …)` revision markers from the LEG-010/011/013/015/016/017/027
  issue titles. Kept the roadmap phase labels `R-1 — Contracts v1` (release/phase
  names, not spec revisions).
- **`README.md`** — removed `— LEG-048` (R-2 row). Kept "still the v1
  implementations" at line 43 — legitimate *current* code-vs-schema migration
  statement, not obsolete design history.
- **`docs/AGENT_LIFECYCLE.md`** (the core design doc, the largest surface) —
  stripped every provenance marker, keeping decisions as plain current prose:
  - Blockquotes: §4.10 `Reconciled against §4.11 Schema 1 (Session 14) — old
    draft's …` → current statement; §4.11 `Status … Session 14, journals …
    addenda AB–AP, revised after S4 …` → current "as they stand now, validated,
    consistent with §4.10"; §12 `Reconciled … Session 14 … old wording …
    superseded` → current token description.
  - Addenda letters removed: T #1, AY, AT (×2, incl. the P-A ruling refs), AX,
    AM (×3), AJ (Schema 2 / addendum AJ), AJ/AL (×2), AV, AG–AM, AE,
    K/L/M/N; `(S4-proposed)` and `(S2 ruling)` annotations.
  - Headers de-annotated: `(confirmed)` / `(creation rule, confirmed)` on
    §4.1/4.2/4.3/4.4/4.6/4.7/4.8; `(resolved 2026-08-30)` / `(confirmed
    2026-08-30)` / `(resolved 2026-09-02)` date-stamps on decision items and
    invariants; `(reconciled thread 3)`; `(Resolved — see §10, open decision 3)`.
  - `— H3` hypothesis-provenance marker in the fan-in text removed; the 
    journal-thread pointer `docs/JOURNALS/2026-08-30.md (execution-mechanics
    thread)` removed.
  - Kept legitimate current verb usage of "confirmed"/"resolved" (e.g. "confirmed
    running", "resolved against the chain") and the live `H1–H4` cross-references
    to `docs/VALIDATIONS/single-node-model.md` (current findings, not history).

## Logging

No code change required logging events (this was doc/spec cleanup plus docstring
label removals — no observable-runtime behaviour changed), so no events added or
removed (rule 11 unaffected).

## Tests run

- `uv run python -m pytest -q` → **126 passed**.
- `uv run ruff check src tests` → **All checks passed**.
- `uv run pyright` → **0 errors, 0 warnings, 0 informations**.

## Decisions taken

- **No addendum of any kind** in specs/contracts/design (maintainer ruling):
  decisions are written directly into the text; there is no provenance trail
  outside `docs/JOURNALS/`.
- **History lives only in `docs/JOURNALS/`** — journals are never edited; the
  residual `LEG-048`/addenda mentions that remain are confined to journal files.
- Schema/version labels that denote *current* state (e.g. Schema 1/2/3, the
  roadmap `R-1 Contracts v1`) are kept; labels that record *revision history*
  are removed.
- The live `H1–H4` validation cross-references are current findings and remain.

## Known issues / open points

- This is a broad, maintainer-directed purge; expect maintainer review before
  the work tree is committed/closed. It is authored and verified by the agent.
- The pending `branch_id` fan-in slot-identity bug (reproduced earlier, contract
  not yet approved) is **not** addressed this turn — the purge is a prerequisite
  cleanup so the coming implementation anchors to current-only specs.

## Next steps

- Wait for maintainer review of the purge, then commit the work tree as it
  stabilises (the journal entry is committed within this turn per rule 3).
- After approval: implement `branch_id` (contract-first: update LEG-041 spec +
  add a failing multi-step-branch contract test, then the `_fan_in` fix) on top
  of the now-aligned, history-free runner.

---

# 2026-09-02 — Session 17 — LEG-041 `branch_id` (fan-in slot identity fix)

## Scope

Implement the maintainer-approved `branch_id` schema field that fixes the
confirmed parallel fan-in bug: a parallel's `_fan_in` slotted results by
`request.level_route[0]`, but a **composite (multi-step) branch** re-seeds with
an expanded `level_route` (its own sequence stages), so its returning result's
`level_route[0]` is the branch's first *step* — not the branch class name —
causing the slot lookup to miss and raise
`ValueError: parallel 'par': unknown branch ….` Contract-first (TDD):
spec + failing test first, implementation then green.

## Work done

- **Red** — `tests/test_leg041_parallel_agent.py`: added
  `test_parallel_joins_a_multi_step_branch_by_branch_id`, which fans out a
  parallel, then delivers a returning branch result carrying an expanded
  `level_route=("summ_a","summ_b")` but `branch_id="seqbranch"`, and asserts
  the join produces the correctly merged continuation payload. Failed (red) on
  the then-absent `branch_id` field.
- **Spec** — `docs/CONTRACTS/LEG-041-parallel-agent.md`: renamed slot identity to
  `branch_id` (a Schema 2 token field) set by the parallel at fan-out to the
  branch class name and preserved through the branch's execution (base route +
  sequence re-seed); documented that this lets a multi-step branch (whose
  returned `level_route` expands to its internal steps) still slot under the
  parent-assigned id. Verified no ripple to LEG-042/052 — they reference
  LEG-041's task-id/slot design, consistent with `branch_id`.
- **Schema** — `src/legio/flow/messages.py`: added optional
  `branch_id: str = ""` to `ImmutableMessage` (rides on both request and
  result; empty = not a branch); updated the token-field docstring (Schema 2,
  AGENT_LIFECYCLE §4.11).
- **`src/legio/agents/base.py`** — `_route_outcome`: preserved
  `branch_id=request.branch_id` on both the advance `ExecutionRequestMessage`
  and the level-closing `ExecutionResultMessage`.
- **`src/legio/agents/sequence_agent.py`** — `_handle`: preserved
  `branch_id=request.branch_id` when re-seeding the first stage (this is what
  carries the id through a composite branch's internal sequence).
- **`src/legio/agents/parallel_agent.py`** — `_fan_out`: set each child's
  `branch_id=branch_class`; `_fan_in`: slot by `request.branch_id` (falling back
  to `level_route[0]` when empty); updated the fan-in docstring. Module docstring
  updated to name `branch_id` as the slot identity.

## Logging

No new events: the parallel already emits `parallel fan-out` / join events and
the fix only changes which token names the join slot — no observable-runtime
lifecycle changed (rule 11 unaffected).

## Tests run

- `uv run python -m pytest tests/test_leg041_parallel_agent.py -q` → **5 passed**
  (new multi-step-branch test + existing fan-out test now asserts `branch_id`).
- `uv run python -m pytest -q` → **127 passed** (was 126).
- `uv run ruff check src tests` → **All checks passed**.
- `uv run pyright` → **0 errors, 0 warnings, 0 informations**.

## Decisions taken

- `branch_id` is **the parallel branch slot identity**: set at fan-out to the
  branch class name, preserved through the branch's whole execution, and used by
  the fan-in to name the join slot. It is *not* a synthetic/counter id — a
  branch's distinctness comes from its path/slot (LEG-041), and `branch_id ==`
  the branch class name keeps that invariant.
- Backward-compatible: `_fan_in` falls back to `level_route[0]` when `branch_id`
  is empty, so single-class/legacy results continue to join.

## Design docs updated to match (schema + lifecycles + design)

The `branch_id` field definition, the parallel fan-out/fan-in cycle and the
FlowToken design now reflect it, not just the LEG-041 contract:

- `docs/AGENT_LIFECYCLE.md` §4.11 settled-token table: added the `branch_id`
  row; the §4.11 parallel bullet now assigns `branch_id` at fan-out and names
  the join slot by the returning result's `branch_id`; the §12.1 flow-fields
  list now includes `branch_id`.
- `docs/ARCHITECTURE.md` §3 (Schema 2 fields and the parallel fan-out bullet):
  added `branch_id` to the field list and to the parallel flow description.

So the schema, the working cycle and the design all agree with the shipped code.

## Known issues / open points

- (none) — `branch_id` is now covered in the contract, the schema/token table,
  the lifecycle and the architecture.

## Next steps

- Journal committed within this turn (rule 3); the work tree (schema + fix +
  docs) is left uncommitted pending the maintainer-expected review/approval flow.
